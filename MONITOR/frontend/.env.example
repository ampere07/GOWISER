# Must include the /api suffix — the client strips it only when fetching the
# Sanctum CSRF cookie.
REACT_APP_API_BASE_URL=http://localhost:8000/api

# Seconds between silent dashboard refreshes.
REACT_APP_POLL_INTERVAL=30
